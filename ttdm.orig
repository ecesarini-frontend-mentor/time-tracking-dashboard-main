function ttdmGetButtonName(targetClass) {
    var buttons = document.getElementsByClass(targetClass)[0].getElementsByTagName("button");
    for (let i = 0; i <= buttons.length; i++) {
        buttons[i].onclick = function() {
           return this.getElementsByName;
        }; 
}

function ttdmGetJsonResponse(targetClass) {
    var buttonActionClass = document.getElementsByClassName(targetClass);
    var xmlhttp = new XMLHttpRequest();
    xmlhttp.onreadystatechange = function() {
        for (let i = 0; i <= buttonActionClass.length; i++) {
            if (this.readyState == 4 && this.status == 200) {
                var myObj = JSON.parse(this.responseText);
                document.getElementById(buttonName).innerHTML = myObj.data.name;
            }
        }
    };
    xmlhttp.open("GET", "data.json", true);
    xmlhttp.send();
}

function ttdmShowTimeframes() {
    var buttonName = ttdmGetButtonName("fb-profile-intervals");
    for(let i = 0; i <= buttonName.length; i++) {
        switch(buttonName) {
            case "daily": 

            
        }
    }
}

